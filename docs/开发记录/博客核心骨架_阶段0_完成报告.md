# 阶段 0 完成报告 — 基础设施

**功能**：博客核心骨架
**阶段**：阶段 0（基础设施）
**任务**：T-01 — 数据层修复 + Shiki 代码高亮配置
**完成时间**：2026-05-10
**状态**：✅ 完成

---

## 完成摘要

完成数据层四项基础改造：分类字段默认值、日期格式规范化、站点常量扩展、Shiki 代码高亮配置。

## TDD 测试结果

| 测试套件 | 测试数 | 通过 | 失败 |
|---------|--------|------|------|
| `src/utils/date.test.ts` | 3 | 3 | 0 |
| `src/utils/constants.test.ts` | 2 | 2 | 0 |
| `src/content.config.test.ts` | 3 | 3 | 0 |
| **合计** | **8** | **8** | **0** |

## 完成事项

1. **`content.config.ts`**：`category` 从 `z.string().min(1)` 改为 `z.string().default("未分类")`，省略分类时自动归入"未分类"
2. **`date.ts`**：`formatDate` 从 zh-CN 格式（`2026/05/10`）改为手动拼接 YYYY-MM-DD（`2026-05-10`）
3. **`constants.ts`**：新增 `SITE_AVATAR` 和 `SITE_BIO` 导出
4. **`astro.config.mjs`**：新增 `markdown.shikiConfig` 配置（`github-dark-dimmed` 主题 + `wrap: true`）

## 覆盖的 AC

| AC 编号 | 描述 | 状态 |
|---------|------|------|
| AC-017 | 未分类文章 | ✅ |
| AC-025 | category 必填 → "未分类" | ✅ |
| AC-026 | 日期格式 YYYY-MM-DD | ✅ |
| AC-002 | 站点头像和简介常量 | ✅ |
| AC-003 | Shiki 代码高亮配置 | ✅ |

## 已知问题

- `pnpm build` 因预存问题（`SearchBar.tsx` 引用缺失的 `/pagefind/pagefind.js`）失败，与本次变更无关
- `astro check` 中的 `z` deprecation 警告是 zod 4 类型定义与 Astro 内部分歧造成的，不影响功能

## 下一步

阶段 1（T-02）：Header 导航改造 + ThemeToggle 挂载 + 汉堡菜单
