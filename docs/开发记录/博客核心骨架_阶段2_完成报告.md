# 阶段 2 完成报告 — 首页

**功能**：博客核心骨架
**阶段**：阶段 2（首页）
**任务**：T-03 — 首页 Hero 区域 + PostCard 标题截断
**完成时间**：2026-05-10
**状态**：✅ 完成

---

## 完成摘要

首页新增 Hero 区域展示作者头像和简介，PostCard 标题添加单行截断防止长标题撑破布局。

## TDD 测试结果

| 测试套件 | 测试数 | 通过 | 失败 |
|---------|--------|------|------|
| T-01 套件（date/constants/content） | 8 | 8 | 0 |
| T-02 套件（Header） | 4 | 4 | 0 |
| `src/pages/index.test.ts`（T-03） | 2 | 2 | 0 |
| `src/components/PostCard.test.ts`（T-03） | 1 | 1 | 0 |
| **合计** | **15** | **15** | **0** |

## 改动内容

### `src/pages/index.astro`
- 导入 `SITE_AVATAR` 和 `SITE_BIO` 常量
- 在 PostList 上方新增 Hero 区域：圆角头像 `<img>`（80×80）+ 简介 `<p>`
- 响应式布局：移动端垂直居中排列，`sm:` 断点起水平排列左对齐

### `src/components/PostCard.astro`
- 标题 `<h2>` 添加 `line-clamp-1` 类，超长标题在一行内截断显示省略号

## 覆盖的 AC

| AC 编号 | 描述 | 状态 |
|---------|------|------|
| AC-001 | 首页文章列表 | ✅（已有 PostList，不变） |
| AC-002 | 首页个人简介 | ✅ Hero 区显示头像和简介 |
| AC-020 | 超长标题截断 | ✅ line-clamp-1 |

## 验收验证

Dev server 渲染确认：
- Hero `<section>` 含 `<img src="/images/avatar.webp">` + SITE_BIO 文本
- PostCard 标题 `<h2>` 含 `line-clamp-1` 类
- Tailwind 生成的 `line-clamp-1` CSS 在输出中

## 下一步

阶段 3（T-04）：Slug 唯一性校验 + 图片缺失占位
